export class Team {
  constructor(
    public id: number,
    public abbreviation: string,
    public city: string,
    public conference: string,
    public division: string,
    public full_name: string,
    public name: string,

  ) {
  }
}
